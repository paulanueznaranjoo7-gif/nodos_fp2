"""Definición de la clase LinkedList para construir listas enlazadas."""

class LinkedList:
    """Lista simplemente encadenada."""

    class Node:
        """Nodos de la lista."""

        def __init__(self, value, next_node = None):
            """Inicializa el valor y el enlace al siguiente nodo."""

            self.value = value
            self.next_node = next_node
            
    def __init__(self):
        """Inicializa el principio y final a None, y el tamaño a 0."""
        
        self.__first = None
        self.__last  = None
        self.__len = 0
        
    def __len__(self):
        """Devuelve el tamaño de la lista."""
        
        return self.__len
    
    # NO MODIFIQUE ESTA LÍNEA ni el código que está por encima
    # (de lo contrario, no serán fiables los tests automáticos).
    # Escriba su solución a partir de la siguiente línea.
    
    def __iter__(self):
        current = self.__first         
        while current is not None: 
            value = current.value 
            current = current.next_node 
            yield value
    
    def insert_last(self, value):
        new_node = self.Node(value)
        if self.__first is None:
            self.__first = new_node
            self.__last = new_node
        else:
            self.__last.next_node = new_node
            self.__last = new_node
            self.__len += 1
    
    def filtrar(self, n):
        current = self.__first
        nueva = LinkedList()
        while current is not None:
            if len(current.value) > n:
                nueva.insert_last(current.value)
            current = current.next_node
        return nueva
                
                
                
if __name__ == "__main__":
    lista_enlazada = LinkedList()

    # Insertamos datos (necesario para probar filtrar)
for palabra in ["esto", "es", "una", "lista", "de", "ejemplo"]:
    lista_enlazada.insert_last(palabra)

resultado = lista_enlazada.filtrar(3)

print("Original:", end=" ")
for x in lista_enlazada:
    print(x, end=" -> ")

print("\nFiltrada (n=3):", end=" ")
for x in resultado:
    print(x, end=" -> ")
